function init(tabId,changeInfo,tab){if(chrome.pageAction.hide(tabId),"complete"==tab.status&&tab.url.indexOf("AdminConsole")){var worker=chrome.tabs.connect(tab.id,{name:"BC"});worker.onMessage.addListener(function(msg){console.log(msg,tab),msg&&msg.data&&chrome.pageAction.show(tab.id)}),worker.postMessage({action:"Page:getContentType"})}}chrome.tabs.onUpdated.addListener(init);